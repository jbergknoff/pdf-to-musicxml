services:
  # Bun toolchain for the editor (build, format, lint, typecheck, unit tests).
  # The pdf-to-musicxml app has its own docker-compose under import-image/.
  main:
    image: oven/bun:${BUN_VERSION:-1}
    command: echo
    volumes:
      - ${PWD}:${PWD}
    working_dir: ${PWD}
